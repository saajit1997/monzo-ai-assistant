# OPENAI_API_KEY is unused -- kept as a placeholder in case a future phase needs it.
OPENAI_API_KEY=

# Required from Phase 7 onward: app/streamlit_app.py calls the Claude API to
# generate grounded answers. Get a key at https://console.anthropic.com/
# and put it in a local .env (never commit .env -- it's gitignored).
ANTHROPIC_API_KEY=
